import { getElement } from './index.js'

describe('integrateElement()', () => {
  test('should ', () => {
    console.log(getElement);
  })
});

describe('getElement()', () => {
  test('should ', () => {

  });
})