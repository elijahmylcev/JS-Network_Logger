const index = require('./index');
// import index from './index.js'

describe('integrateElement()', () => {
  test('should ', () => {

  })
});

describe('getElement()', () => {
  test('should ', () => {

  });
})